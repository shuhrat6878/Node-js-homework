const fs  = require('fs');

fs.readFile ('text.txt' , "utf-8", (err,data)=>{
    if(arr){
        console.log(arr);
        return
    }
    console.log("bu data", data)

})

const data = fs.promises.ran